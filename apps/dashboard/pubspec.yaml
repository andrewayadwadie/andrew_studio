name: dashboard
description: Andrew Studio Web Dashboard (Jaspr)
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  jaspr: ^0.22.0
  http: ^1.2.2
  supabase: ^2.3.6
  web: ^1.0.0

dev_dependencies:
  jaspr_builder: ^0.22.0
  build_runner: ^2.4.13
  build_web_compilers: ^4.0.0
  test: ^1.25.2
  jaspr_test: ^0.22.0

jaspr:
  mode: client
